export async function uploadToSupabase(file:File,path:string){const base=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!base||!key)throw new Error('Supabase Storage is not configured');const r=await fetch(`${base}/storage/v1/object/products/${path}`,{method:'POST',headers:{Authorization:`Bearer ${key}`,apikey:key,'Content-Type':file.type},body:Buffer.from(await file.arrayBuffer())});if(!r.ok)throw new Error(await r.text());return `${base}/storage/v1/object/public/products/${path}`}
