'use client'; import {useState} from 'react'; export default function AddToCart({productId}:{productId:string}){const[loading,setLoading]=useState(false);const add=async()=>{setLoading(true);const r=await fetch('/api/cart',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({productId,quantity:1})});setLoading(false);if(r.status===401)location.href='/login?next=/products';else alert(r.ok?'Added to cart':'Unable to add to cart')};return <button className="btn primary" onClick={add} disabled={loading}>{loading?'Adding…':'Add to cart'}</button>}
