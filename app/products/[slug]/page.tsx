import Link from 'next/link'
import { notFound } from 'next/navigation'
import { prisma } from '@/lib/prisma'
import { money } from '@/lib/utils'

export default async function ProductDetail({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
  const products = await prisma.$queryRawUnsafe<Array<{ id: string; name: string; slug: string; description: string | null; price: number; brand: string | null; category: string }>>('SELECT p.id,p.name,p.slug,p.description,p.price,b.name AS brand,c.name AS category FROM public."Product" p LEFT JOIN public."Brand" b ON b.id=p."brandId" JOIN public."Category" c ON c.id=p."categoryId" WHERE p.slug=$1 AND p."isActive"=TRUE AND p."deletedAt" IS NULL LIMIT 1', slug)
  const product = products[0]
  if (!product) notFound()
  const images = await prisma.$queryRawUnsafe<Array<{ url: string; altText: string | null }>>('SELECT url,"altText" FROM public."ProductImage" WHERE "productId"=$1::uuid ORDER BY "isPrimary" DESC NULLS LAST', product.id)
  return <main className="product-detail-page"><header className="detail-header"><div className="detail-container detail-nav"><Link className="detail-logo" href="/"><span>AmakTech</span> <b>Connect</b></Link><nav><Link href="/products">Shop</Link><Link href="/account">Account</Link><Link href="/cart">Cart</Link><Link className="detail-signin" href="/login">Sign in</Link></nav></div></header><div className="detail-container detail-content"><Link href="/products" className="back-link">← Back to shop</Link><div className="detail-grid"><div className="detail-gallery">{images[0] ? <img src={images[0].url} alt={images[0].altText || product.name} /> : <div className="detail-placeholder">{product.name.slice(0,1)}</div>}</div><section className="detail-copy"><span className="detail-category">{product.category}</span><p className="detail-brand">{product.brand || 'AmakTech Connect'}</p><h1>{product.name}</h1><p className="detail-price">{money(product.price)}</p><div className="detail-rule" /><p className="detail-description">{product.description || 'Quality products selected for modern living.'}</p><form className="add-form" method="post" action="/api/cart"><label htmlFor="quantity">Quantity</label><div className="add-row"><input id="quantity" name="quantity" type="number" min="1" defaultValue="1" /><input type="hidden" name="productId" value={product.id} /><button type="submit">Add to cart <span>→</span></button></div></form><div className="detail-benefits"><span>✓ Quality checked</span><span>✓ Secure checkout</span><span>✓ Flexible delivery</span></div></section></div></div><style>{`.product-detail-page{min-height:100vh;background:#f7f5f5;color:#080808}.detail-container{width:min(1200px,calc(100% - 40px));margin:0 auto}.detail-header{background:#fff;border-top:6px solid #080808;border-bottom:1px solid #e7e2e3}.detail-nav{min-height:88px;display:flex;align-items:center;justify-content:space-between;gap:24px}.detail-logo{font-size:27px;font-weight:950;letter-spacing:-1.5px}.detail-logo span{color:#319b19}.detail-logo b{color:#d8a900}.detail-nav nav{display:flex;align-items:center;gap:26px;font-size:14px;font-weight:700}.detail-nav nav a:hover{color:#319b19}.detail-signin{color:#319b19}.detail-content{padding:48px 0 85px}.back-link{color:#716c6e;font-size:14px;font-weight:700}.back-link:hover{color:#319b19}.detail-grid{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(350px,.95fr);gap:78px;align-items:center;margin-top:34px}.detail-gallery{background:#ebe8e9;border-radius:18px;overflow:hidden;min-height:590px;display:grid;place-items:center}.detail-gallery img{width:100%;height:590px;object-fit:contain;mix-blend-mode:multiply}.detail-placeholder{font-size:170px;font-weight:950;color:#319b19}.detail-category{display:inline-block;background:#eaf4e8;color:#277c18;border-radius:999px;padding:8px 12px;text-transform:uppercase;letter-spacing:1px;font-size:10px;font-weight:800}.detail-brand{color:#716c6e;font-size:14px;margin:24px 0 0}.detail-copy h1{font-size:clamp(44px,5.5vw,78px);line-height:.95;letter-spacing:-4px;margin:13px 0 18px}.detail-price{font-size:25px;font-weight:800;margin:0}.detail-rule{height:1px;background:#ded8d9;margin:28px 0}.detail-description{color:#716c6e;line-height:1.6;max-width:490px}.add-form{margin-top:35px}.add-form label{display:block;color:#716c6e;font-size:12px;font-weight:700;margin-bottom:9px}.add-row{display:flex;gap:10px}.add-row input{width:78px;padding:15px;border:1px solid #d8d2d3;border-radius:8px;background:#fff;font:inherit}.add-row button{flex:1;border:0;border-radius:999px;background:#080808;color:#fff;padding:15px 22px;font:inherit;font-weight:800;cursor:pointer}.add-row button:hover{background:#319b19}.add-row button span{color:#d8a900;margin-left:10px;font-size:19px}.detail-benefits{display:flex;flex-wrap:wrap;gap:13px 20px;margin-top:25px;color:#716c6e;font-size:12px}.detail-benefits span:first-child{color:#319b19}@media(max-width:800px){.detail-nav{align-items:flex-start;flex-direction:column;padding:22px 0}.detail-nav nav{width:100%;justify-content:space-between;gap:12px}.detail-grid{grid-template-columns:1fr;gap:34px}.detail-gallery{min-height:400px}.detail-gallery img{height:400px}.detail-copy h1{letter-spacing:-2px}}@media(max-width:480px){.detail-container{width:calc(100% - 28px)}.detail-content{padding-top:35px}.detail-gallery{min-height:330px}.detail-gallery img{height:330px}.add-row{align-items:stretch}.add-row button{padding-left:14px;padding-right:14px}}
    `}</style></main>
}
