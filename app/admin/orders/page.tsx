import Link from 'next/link'; import {requireAdmin} from '@/lib/auth'; import {prisma} from '@/lib/prisma'; import {money} from '@/lib/utils';
export default async function AdminOrders(){await requireAdmin();const os=await prisma.$queryRawUnsafe<Array<{id:string;name:string;total:number;status:string}>>('SELECT o.id, u.name, o.total, o.status FROM public."Order" o JOIN public."User" u ON u.id = o."userId" WHERE o."deletedAt" IS NULL ORDER BY o."createdAt" DESC');return <main className="container" style={{paddingTop:40}}><h1>Orders</h1><div className="grid">{os.map(o=><Link className="card" href={`/admin/orders/${o.id}`} key={o.id}><b>Order {o.id.slice(0,8)}</b><p>{o.name} • {money(o.total)} • {o.status}</p></Link>)}</div></main>}
